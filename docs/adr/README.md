# Architecture decision records

Each file records one decision, why it was made, and what it costs us. They are
append only. If a decision is reversed, add a new record that supersedes the old
one rather than editing history.

Numbering is sequential. Status is one of `accepted`, `superseded by NNNN`, or
`proposed`.
